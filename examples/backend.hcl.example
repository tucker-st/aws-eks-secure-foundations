# examples/backend.hcl.example
#
# Copy to examples/backend.hcl (untracked) and run:
#   terraform init -reconfigure -backend-config=examples/backend.hcl
#
# Never commit real backend.hcl values.

bucket         = "REPLACE_WITH_YOUR_STATE_BUCKET"
key            = "eks/REPLACE_WITH_ENV.tfstate"
region         = "us-east-1"
encrypt        = true
# dynamodb_table = "REPLACE_WITH_YOUR_LOCK_TABLE"

